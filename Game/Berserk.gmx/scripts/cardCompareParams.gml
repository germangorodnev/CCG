///cardCompareParams(CARD* id, type, hp, armor, dmg, mana)
with (argument0)
{
    if (type == argument1 && hp == argument2 && armor == argument3 && dmg == argument4 && mana == argument5)
        return true;
    else
        return false;
}
